import { Box, Typography } from "@mui/material";

export const FormatLessBoldIcon = () => {
  return (
    <Box sx={{ width: "24px" }}>
      <Typography sx={{ fontWeight: 100 }}>B</Typography>
    </Box>
  );
};
